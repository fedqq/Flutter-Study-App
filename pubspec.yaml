name: studyappcs
description: "A new Flutter project."

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.19.0
  fl_chart: ^0.68.0
  flutter_material_color_picker: ^1.2.0
  share_plus: ^9.0.0
  path_provider: ^2.1.3
  fuzzywuzzy: ^1.1.6
  pdf: ^3.10.8
  window_rounded_corners: ^0.0.3
  dynamic_color: ^1.7.0
  firebase_auth: ^5.1.0
  firebase_core: ^3.1.0
  cloud_firestore: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
